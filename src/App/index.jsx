import React, { Component } from 'react';
import SimpleRouter from '../Components/RouterExample';

export default class App extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <SimpleRouter />
    );
  }
}